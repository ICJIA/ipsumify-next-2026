/**
 * Nuxt UI app configuration.
 *
 * Defines color scheme for Nuxt UI components (primary, neutral).
 * Theme colors used in meta tags come from ipsumify.config.ts.
 *
 * @module app.config
 */
export default defineAppConfig({
  ui: {
    colors: {
      primary: 'teal',
      neutral: 'zinc'
    }
  }
})
